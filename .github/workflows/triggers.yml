name: Triggers CI

on:
  push:
    branches: [ main, 'feature/**' ]
    paths-ignore: [ 'docs/**' ]
  pull_request:
    branches: [ main ]

jobs:
  triggers:
    name: Triggers
    runs-on: ubuntu-latest
    steps:
      - run: echo "Wywołanie Workflow'u Triggers"