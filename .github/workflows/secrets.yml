name: Secrets

on:
  push:
  workflow_dispatch:

jobs:
  use-secret:
    runs-on: ubuntu-latest
    steps:
      - name: Użyj sekretu
        env:
          TOKEN: ${{ secrets.API_TOKEN }}
        run: 'echo "Długość Tokenu: ${#TOKEN}"'
        # Dla bezpieczeństwa